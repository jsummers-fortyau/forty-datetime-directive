chai = require 'chai'
sinon = require 'sinon'

{Fortydatetime} = require '../src/forty-datetime'

chai.should()

describe 'forty-datetime', ->
  it 'should do something', ->
    true.should.equal true